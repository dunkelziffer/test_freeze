// App-level Avo customizations, loaded after avo/application.js so they can
// register Stimulus controllers. Avo's layout links both this and the
// stylesheet below unconditionally, so both must exist even when empty.
import './avo-overrides.css'
