{
  "$comment": [
    "Assets owned by gems that expect a Rails asset pipeline, mapped into our esbuild build.",
    "Shared by esbuild.config.js and test/assets/asset_mappings_test.rb; `bundle info <gem> --path`",
    "is the only runtime link between them. Paths are relative to the gem root.",
    "",
    "assetPaths mirrors `config.assets.paths`: where a gem's assets are found, and what the audit",
    "test asserts. manifestEntries is what Ruby names and so needs a manifest key. The two do not",
    "line up — assets referenced from a gem's own CSS or JS arrive through assetPaths alone.",
    "",
    "logicalName is the helper argument minus its extension; `*` becomes the file's basename.",
    "",
    "The audit sees only explicit registrations; Propshaft also adds each engine's app/assets/*",
    "implicitly. We ship our own avo-overrides rather than rely on that."
  ],
  "gems": [
    {
      "gem": "avo",
      "railsEnvironments": ["development", "test"],
      "assetPaths": [
        {
          "$comment": "Prebuilt CSS and JS, under the logical names Avo's layout asks for.",
          "path": "app/assets/builds"
        },
        {
          "$comment": "Logos and favicons. Also resolves the webfonts: avo/application.css asks for url(fonts/…), meaning logical avo/fonts/…",
          "path": "app/assets/images"
        },
        {
          "$comment": "Icons avo-icons inlines from disk. Never fetched over HTTP, so no manifestEntries.",
          "path": "app/assets/svgs"
        },
        {
          "$comment": "Sprockets-style path for a bare fonts/… lookup. Unused here; listed to match what Avo registers.",
          "path": "app/assets/images/avo"
        }
      ],
      "manifestEntries": [
        {
          "$comment": "e.g. stylesheet_link_tag \"avo/application\". Also builds the unreferenced avo.custom.js.",
          "sourceGlob": "app/assets/builds/avo/*",
          "logicalName": "avo/*"
        },
        {
          "$comment": "e.g. image_tag \"avo/logo.png\". Webfonts one directory down are excluded: the stylesheet already pulls them in.",
          "sourceGlob": "app/assets/images/avo/*",
          "logicalName": "avo/*"
        }
      ]
    }
  ]
}
